Перед запуском проекта заполнить .env файл в соответствии с docker compose.
Можно использовать как шаблон .env.example.

Запуск проекта локально: 
```
docker compose -f docker-compose.local.yml up --build -d
```